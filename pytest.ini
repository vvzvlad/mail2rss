[pytest]
testpaths = tests
# project root on sys.path so `import src...` works
pythonpath = .
# auto: every `async def test_*` runs on the event loop without an explicit marker
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q
